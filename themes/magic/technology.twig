{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main technology" role="main">
        <aside class="menu">
            <h1>Meny</h1>
            {% for page in pages("technology") %}
               <p class="{{ current_page.url == page.url ? 'active ' ~ page.meta.class : page.meta.class }}">
                    <a class="{{page.meta.class}}" href="{{ page.url }}">{{ page.title }}</a>
               </p>


                <!--<a class="{{ current_page.url == page.url ? 'active' : "" }}"
                    href="{{ page.url }}"><p class="{{ page.meta.class }} "> {{ page.title }}</p>
                </a>
                
                    
                     <p class="{{ page.meta.class }}">{{ page.title }}</p> -->
                
            {% endfor %}
        </aside>

        <article class="article">
            {{ content }}
        </article>
    </div>

    {% include 'incl/footer.twig' %}
</body>

</html>